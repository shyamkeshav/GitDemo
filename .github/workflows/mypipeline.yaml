
name : my_pipeline

on :
  workflow_dispatch :

jobs:
  test-jobs-for-learning:
    runs-on : ubuntu-latest
    steps:
      - name : run the command
        run : echo " Hi From console"

  test-jobs-for-learning-python:
    runs-on : ubuntu-latest
    steps:
      - run : print("Hello world!")
        shell : python 
    needs : test-jobs-for-learning         