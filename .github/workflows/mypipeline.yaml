name: practise_pipeline

on:
  workflow_dispatch:

jobs:
  pipeline_learning:
    runs_on : ubuntu-latest

    steps:
      - name : checking the flow
        run : echo "This will be printed on the console"
