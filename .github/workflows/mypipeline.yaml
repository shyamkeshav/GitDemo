name : test-pipeline

on:
  workflow_dispatch

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run : echo "this is an echo output"

  job2:
    runs-on : ubuntu-latest
    steps:
      - run : print ("Hello python")
        shell: python
    needs : job1    